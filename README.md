# Sortio

Minimal Plug-based API.

## Setup

```bash
mix deps.get
```

## Run

```bash
mix run --no-halt
```

Server starts on `http://localhost:4000`

## Test

```bash
mix test
```

## Endpoints

- `GET /health` - Health check
